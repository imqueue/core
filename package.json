{
  "name": "imq",
  "version": "1.2.2",
  "description": "Simple JSON-based messaging queue for inter service communication",
  "scripts": {
    "prepublish": "./node_modules/.bin/tsc",
    "test": "./node_modules/.bin/tsc && ./node_modules/.bin/nyc mocha && /usr/bin/env node -e \"require('opn')('file://`pwd`/coverage/index.html');process.exit(0)\"",
    "test-dev": "npm run test && npm run clean-js && npm run clean-typedefs && npm run clean-maps",
    "clean-typedefs": "find . -name '*.d.ts' -not -wholename '*node_modules*' -not -wholename '*generator*' -type f -delete",
    "clean-maps": "find . -name '*.js.map' -not -wholename '*node_modules*' -not -wholename '*generator*' -type f -delete",
    "clean-js": "find . -name '*.js' -not -wholename '*node_modules*' -not -wholename '*generator*' -type f -delete",
    "clean-tests": "rm -rf .nyc_output coverage",
    "clean-doc": "rm -rf docs",
    "clean-benchmark": "rm -rf benchmark-result",
    "clean": "npm run clean-tests && npm run clean-typedefs && npm run clean-maps && npm run clean-js && npm run clean-doc && npm run clean-benchmark",
    "doc": "rm -rf docs && typedoc  --excludePrivate --excludeExternals --hideGenerator --exclude \"**/+(test|node_modules|docs|coverage|benchmark|.nyc_output)/**/*\" --mode file --out ./docs . && /usr/bin/env node -e \"require('opn')('file://`pwd`/docs/index.html');process.exit(0)\""
  },
  "repository": {
    "type": "git",
    "url": "git://github.com/Mikhus/imq.git"
  },
  "author": "Mykhailo Stadnyk <mikhus@gmail.com> (https://github.com/Mikhus)",
  "license": "ISC",
  "dependencies": {
    "@types/microtime": "^2.1.0",
    "@types/node": "^9.4.6",
    "@types/redis": "^2.8.6",
    "microtime": "^2.1.7",
    "redis": "^2.8.0",
    "reflect-metadata": "^0.1.12",
    "source-map-support": "^0.5.3",
    "ts-node": "^5.0.1",
    "typedoc": "^0.10.0",
    "typescript": "^2.7.2"
  },
  "devDependencies": {
    "@types/chai": "^4.1.2",
    "@types/memwatch-next": "^0.3.1",
    "@types/mocha": "^2.2.48",
    "@types/mock-require": "^2.0.0",
    "@types/sinon": "^4.3.0",
    "@types/yargs": "^11.0.0",
    "chai": "^4.1.2",
    "memwatch-next": "^0.3.0",
    "mocha": "^5.0.1",
    "mock-require": "^3.0.1",
    "nodeaffinity": "^0.1.0",
    "nyc": "^11.4.1",
    "opn": "^5.2.0",
    "sinon": "^4.4.2",
    "yargs": "^11.0.0"
  },
  "main": "index.js",
  "typescript": {
    "definitions": "index.d.ts"
  },
  "nyc": {
    "extension": [
      ".ts"
    ],
    "exclude": [
      "**/*.d.ts",
      "**/test/**"
    ],
    "require": [
      "ts-node/register"
    ],
    "reporter": [
      "text",
      "html"
    ]
  }
}
